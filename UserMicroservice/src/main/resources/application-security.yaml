security:
  oauth2:
    client:
      registration:
        keycloak:
          provider: keycloak
          client-id: users
          client-secret: cKA0bJ3dVU62zMm3Pald5DU23zpg94vx
          authorization-grant-type: client_credentials
          scope: openid, profile
      provider:
        keycloak:
          issuer-uri: http://localhost:8899/realms/leaderboard-security
    resourceserver:
      jwt:
        issuer-uri: http://localhost:8899/realms/leaderboard-security
